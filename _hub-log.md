# Soara知识库 操作日志

---

## [规则变更] 2025-05-16 - 修复子库模板 wiki 文件

**变更摘要**：
- 修复 `_子库模板/wiki/index.md`：移除占位符，添加默认值和注释
- 修复 `_子库模板/wiki/log.md`：移除占位符，添加默认值和注释

**修复原因**：wiki 索引和日志文件包含 `{{xxx}}` 占位符，创建子库后需要手动修改

---

## [规则变更] 2025-05-16 - 修复测试发现的 BUG

**变更摘要**：
- 修复 `wiki/_index.md`：添加编程子库信息，更新统计数据
- 修复 `wiki/编程/`：创建完整的子库目录结构（concepts, entities, queries, comparisons, health）

**修复原因**：测试发现全局索引未更新，编程子库缺少必要的 wiki 子目录

---

## [规则变更] 2025-05-16 - 修复执行 AI 权限漏洞

**变更摘要**：
- 修改 `rules/core.md`：在禁止行为清单中添加 `_子库模板/` 保护
- 明确执行 AI 不得修改 `_子库模板/` 下任何文件（包括 `.claude.json` 和 `rules/local.md`）

**修复原因**：`_子库模板/` 包含配置文件和规则文件，但不在 `rules/` 目录下，执行 AI 可以绕过权限限制进行修改

---

## [规则变更] 2025-05-16 - 升级到 v3.8

**变更摘要**：
- 版本升级：3.6 → 3.8
- 新增规则文件：`security.md`、`backup.md`
- 新增目录：`_backups/`、`_temp/`
- 更新 `.claude.json`：添加 `current_user`、`large_file_action`、`protected_rules`、`auto_sync_child_pages`
- 新增命令别名：`help`/`帮助`、`backup`/`备份`、`auto-fix`/`自动修复`
- 更新 `CLAUDE.md` 和 `AGENTS.md` 到 v3.8

**简化内容**：移除向量索引、多用户权限、自动定时备份等过度工程设计

**强化机制**：增量编译、父子页关联、事务性写入、自动修复、手动备份

---

## [规则变更] 2025-05-16 - 修复子库模板

**变更摘要**：
- 修复 `_子库模板/.claude.json`：移除占位符，更新版本号为 3.8，添加 `current_user`
- 修复 `_子库模板/CLAUDE.md`：添加初始化检查清单和目录结构说明
- 修复 `_子库模板/rules/local.md`：添加初始化指引模板
- 新增 `_子库模板/raw/` 目录

**修复原因**：模板文件包含占位符 `{{xxx}}`，创建子库后需要手动修改，用户体验差

---

## 迁移报告：F:\Obsidian\小说知识库 → Soara知识库/小说知识库

> 迁移日期：2025-05-16
> 源路径：F:\Obsidian\小说知识库
> 目标路径：E:\WorkBuddy\创建规则大师\wiki\小说知识库

---

## 一、迁移完成情况

### 已完成

| 项目 | 数量 | 状态 |
|------|------|------|
| raw/ 源文件 | 67 个 | ✅ 已复制 |
| wiki/concepts/ 概念页 | 74 个 | ✅ 已复制 |
| wiki/entities/ 实体页 | 2 个 | ✅ 已复制 |
| 目录结构创建 | 5 个子目录 | ✅ 已创建 |

### 目录结构

```
wiki/小说知识库/
├── CLAUDE.md
├── .claude.json
├── rules/
│   └── local.md
├── raw/                    ← 67 个源文件
├── wiki/
│   ├── index.md
│   ├── log.md
│   ├── concepts/           ← 74 个概念页
│   ├── entities/           ← 2 个实体页
│   ├── queries/            ← 空目录
│   ├── comparisons/        ← 空目录
│   └── health/             ← 空目录
└── output/ (待创建)
```

---

## 二、待重新编译项

### 概念页格式调整（74 个）

每个概念页需要按 v3.6 标准重新编译：

| 调整项 | 原格式 | v3.6 格式 | 优先级 |
|--------|--------|----------|--------|
| YAML `quality_score` | 无 | 添加 0-100 评分 | 高 |
| YAML `source_ref` | 无 | 添加 `"../../raw/文件名.md"` | 高 |
| YAML `related` 格式 | `[["概念"]]` | `"[[概念]]"` | 中 |
| `## 核心定义` | `## 核心结论` | 重命名 | 中 |
| `## 关键特性` | 无 | 添加（必须） | 高 |
| `## 提炼` | `## [[AI推理]] 提炼` | 重命名 | 中 |
| `## 质疑` | 大多无 | 添加（有内容则写） | 高 |
| `## 推理` | 大多无 | 添加（有内容则写） | 高 |
| `## 关联` | 大多无 | 添加（有内容则写） | 高 |
| `## 矛盾检测` | 大多无 | 添加（有内容则写） | 高 |
| `## 参考文献` | 无 | 添加（必须） | 高 |

### 重新编译命令

```
compile 小说知识库 --force
```

---

## 三、v3.6 标准概念页模板

```markdown
---
title: {概念名}
aliases: [别名]
tags: [小说, 类型]
category: concepts
area: 小说
topic: 具体主题
quality_score: 75
source_ref: "../../raw/原文件名.md"
related:
  - "[[相关概念A]]"
  - "[[相关概念B]]"
created: YYYY-MM-DD
updated: YYYY-MM-DD
---

# {概念名}

## 核心定义

{一句话定义，不超过 2 行}

## 关键特性

- 特性 1
- 特性 2
- 特性 3

## 详细阐述

{可选}

## 提炼

{五步分析结果，有内容则写}

## 质疑

{有内容则写}

## 推理

{有内容则写}

## 关联

{有内容则写}

## 矛盾检测

{有内容则写}

## 参考文献

- 源文件：[[../../raw/文件名.md]]
```

---

## 四、后续步骤

1. **执行重新编译**
   ```
   compile 小说知识库 --force
   ```

2. **更新子库索引**
   - 更新 `wiki/小说知识库/wiki/index.md` 统计信息

3. **更新大库全局索引**
   - 更新 `wiki/_index.md` 子库列表

4. **创建 emergence/ 目录**
   - 已在大库根目录创建

5. **健康检查**
   ```
   health 小说知识库
   ```

---

## 五、迁移统计

| 维度 | 数值 |
|------|------|
| 源文件总数 | 67 |
| 概念页总数 | 74 |
| 实体页总数 | 2 |
| 需重新编译 | 74 个概念页 |
| 预计耗时 | 约 2-3 小时（AI 逐个编译） |

---

*报告生成时间：2025-05-16 15:31*
