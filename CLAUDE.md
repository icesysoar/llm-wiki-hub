# CLAUDE.md — 大库速查

> **铁律**：执行 AI 只读规则，规则修改由用户显式触发 `edit-rule` 命令进入编辑模式
> **版本**：v4.2.1

---

## 目录结构

```
我的知识库/
├── raw/              ← 收集箱（分拣后移动，不保留）
├── rules/            ← 规则目录（4个文件，执行AI只读）
├── wiki/             ← 子库容器
│   └── [子库]/
│       ├── raw/      ← 分拣后接收
│       └── wiki/     ← 编译产物
├── emergence/        ← 涌现笔记（顶级知识产出）
├── output/           ← 项目空间
└── _system/          ← 系统辅助
    ├── _trash/       ← 回收站
    ├── _backups/     ← 备份存档
    ├── _temp/        ← 临时文件
    ├── _hub-health/  ← 健康报告
    └── _templates/   ← 子库模板
```

---

## 规范文件索引

| 文件 | 内容 | 何时阅读 |
|------|------|---------|
| `rules/hub.md` | 大库规范、铁律、模式切换 | **每次操作前** |
| `rules/core.md` | 标签白名单、匹配策略、禁止行为 | **每次操作前** |
| `rules/operations.md` | 预处理、编译、涌现、迁移、安全 | 执行具体操作时 |
| `rules/output.md` | 输出模板、YAML规范、日志格式 | 生成输出文件时 |

---

## 常用命令

### 大库命令
| 命令 | 别名 | 用途 |
|------|------|------|
| `create-sub <名称> --area <领域>` | `创建子库` | 创建子库 |
| `list-subs` | `子库列表` | 列出子库 |
| `sort-raw` | `分拣raw` | 手动分拣 |
| `hub-health` | `大库健康` | 健康报告 |
| `edit-rule <规则名>` | `编辑规则` | 进入规则编辑模式 |
| `auto-fix [--dry-run]` | `自动修复` | 自动修复问题 |
| `undo [--steps N]` | `撤销` | 撤销操作（通过 Git） |
| `help [command]` | `帮助` | 显示帮助 |

### 子库命令
| 命令 | 别名 | 用途 |
|------|------|------|
| `compile <子库>` | `编译` | 编译子库 |
| `compile-all` | `全部编译` | 编译所有变动子库 |
| `health <子库>` | `健康` | 子库健康报告 |
| `compare <A> <B>` | `对比` | 生成对比页 |
| `lint <子库>` | `检查` | 检查子库 |

### 涌现命令
| 命令 | 别名 | 用途 |
|------|------|------|
| `emerge --pairs A,B` | `涌现 --对` | 分析两个子库 |
| `emerge --all` | `涌现 --全库` | 全库扫描 |
| `emerge --auto` | `涌现 --增量` | 增量涌现 |
| `promote-emergence <文件>` | `提升涌现` | 提升为正式概念 |

---

## 禁止行为（执行 AI）

- ❌ 不得修改 `rules/` 下任何文件
- ❌ 不得修改 `.claude.json`
- ❌ 不得修改 `_system/_templates/` 下任何文件
- ❌ 不得修改任何子库的 `.claude.json` 或 `rules/local.md`
- ❌ 不得在未询问的情况下覆盖用户已有内容
- ❌ 不得自动合并概念
- ❌ **不得自行修复发现的 BUG（必须向用户汇报）**

---

## 备份（Git 方式）

- 日常备份：`git add . && git commit -m "描述"`
- 误操作恢复：`git checkout HEAD -- <文件路径>`

---

**处理任何操作前，请先阅读 `rules/core.md` 获取完整规范。**
